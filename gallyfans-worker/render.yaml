services:
  - type: web
    name: gallyfans-worker
    env: node
    plan: free
    region: oregon
    buildCommand: "npm install && npx prisma generate && npm run build"
    startCommand: "npm start"
    healthCheckPath: /health
    envVars:
      - fromGroup: 4Reels-secrets

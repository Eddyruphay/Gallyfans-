# Gallyfans Environment Variables
# ---------------------------------
# Copie este ficheiro para .env e preencha os valores para desenvolvimento local.
# Este ficheiro é lido pelo 'gateway' e pelo 'wrangler dev'.

# --- Configuração do Gateway (lido por gateway/src/client.ts) ---

# Porta para o servidor da API do Gateway
PORT=3000

# Um token secreto e aleatório para autenticar os workers com o gateway.
# DEVE ser igual ao GATEWAY_AUTH_TOKEN usado pelos workers.
GATEWAY_AUTH_TOKEN="change-this-to-a-secure-random-string"

# O JID do Grupo/Canal do WhatsApp (e.g., 1234567890@g.us) para publicação.
# Usado pelo director-worker para criar jobs.
TARGET_GROUP_ID="your-whatsapp-group-id@g.us"


# --- Configuração dos Workers (lido por wrangler.toml) ---

# URL do gateway local para o delivery-worker enviar os jobs.
GATEWAY_URL="http://localhost:3000"

# --- Opcional: Para o Script de Pareamento (scripts/run-pairing.mts) ---

# O seu número de WhatsApp (e.g., 5511999999999) para gerar um código de pareamento.
WA_PAIRING_NUMBER=""

# Porta para o serviço de pareamento
PAIRING_PORT=3001
